## Гайд на запуск

Должен быть установлен Docker и docker-compose. 

После `git clone` нужно написать команду `docker-compose up --build` в папке проекта и перейти по адресу `http://localhost:8080/docs`